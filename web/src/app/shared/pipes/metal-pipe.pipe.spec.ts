import { MetalPipePipe } from './metal-pipe.pipe';

describe('MetalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MetalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
