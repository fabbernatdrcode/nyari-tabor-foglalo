import { DietaryLabelPipe } from './dietary-label.pipe';

describe('DietaryLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new DietaryLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
